#include <stdio.h>
#include <stdlib.h>
void test();

int main(int argc, char ** argv)
{
    char * foo = "";
    char * gay;
    test("nigger balls");
    char *test[1024];
    strcpy(test, "test");
    
        exit(0);

    if(strcmp("test", test) == 0) {
        printf("Here");
    }

    scanf("%s", test);
    for(int i = 0; i < strlen(test); i++) {
        printf("%s", &test[i]);
    }

}


void test(char * msg)
{
    printf("%s", msg);

}